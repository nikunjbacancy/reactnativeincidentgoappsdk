/**
 *
 * Combine all reducers in this file and export the combined reducers.
 *
 */
import { Action } from 'redux';
declare const rootReducer: (state: any, action: Action) => unknown;
export default rootReducer;
