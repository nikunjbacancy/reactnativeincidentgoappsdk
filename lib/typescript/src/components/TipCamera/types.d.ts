export declare enum TipCameraMode {
    camera = "camera",
    video = "video"
}
export declare enum CameraType {
    back = "back",
    front = "front"
}
export declare enum FlashMode {
    on = "torch",
    off = "off"
}
