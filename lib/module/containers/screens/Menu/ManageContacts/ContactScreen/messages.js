/**
 * ContactScreen messages
 *
 * This contains all the text for the ContactScreen component.
 */

import { defineMessages } from 'react-intl';
export const scope = 'containers.contactScreen';
const messages = defineMessages({
  addContactTitle: {
    id: `${scope}.addContactTitle`
  },
  description: {
    id: `${scope}.description`
  },
  footer: {
    id: `${scope}.footer`
  },
  footerMax: {
    id: `${scope}.footerMax`
  },
  noContacts: {
    id: `${scope}.noContacts`
  },
  noMatches: {
    id: `${scope}.noMatches`
  },
  searchPlaceholder: {
    id: `${scope}.searchPlaceholder`
  },
  title: {
    id: `${scope}.title`
  },
  notifications: {
    id: `${scope}.notifications`
  },
  contactPermissionError: {
    id: `${scope}.contactPermissionError`
  },
  info: {
    id: `${scope}.info`
  },
  warning: {
    id: `${scope}.warning`
  }
});
export default messages;
//# sourceMappingURL=messages.js.map