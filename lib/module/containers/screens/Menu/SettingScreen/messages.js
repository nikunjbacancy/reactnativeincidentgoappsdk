/**
 * NotificationScreen messages
 *
 * This contains all the text for the NotificationScreen component.
 */

import { defineMessages } from 'react-intl';
export const scope = 'containers.notificationScreen';
const messages = defineMessages({
  title: {
    id: `${scope}.title`
  },
  description: {
    id: `${scope}.description`
  },
  warning: {
    id: `${scope}.warning`
  },
  deleteAccount: {
    id: `${scope}.deleteAccount`
  },
  deleteAccountMsg: {
    id: `${scope}.deleteAccountMessage`
  },
  logout: {
    id: `${scope}.logout`
  },
  logoutMessage: {
    id: `${scope}.logoutMessage`
  },
  yes: {
    id: `${scope}.yes`
  },
  no: {
    id: `${scope}.no`
  }
});
export default messages;
//# sourceMappingURL=messages.js.map