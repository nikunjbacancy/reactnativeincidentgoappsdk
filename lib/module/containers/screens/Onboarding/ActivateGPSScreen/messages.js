/**
 * ActivateGPSScreen Messages
 *
 * This contains all the text for the ActivateGPSScreen component.
 */

import { defineMessages } from 'react-intl';
export const scope = 'containers.activateGPSScreen';
const messages = defineMessages({
  title: {
    id: `${scope}.title`
  },
  activateGPS: {
    id: `${scope}.activateGPS`
  },
  continue: {
    id: `${scope}.continue`
  },
  gpsDisabled: {
    id: `${scope}.gpsDisabled`
  },
  gpsEnabled: {
    id: `${scope}.gpsEnabled`
  }
});
export default messages;
//# sourceMappingURL=messages.js.map