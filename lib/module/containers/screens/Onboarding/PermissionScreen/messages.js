/**
 * PermissionScreen Messages
 *
 * This contains all the text for the PermissionScreen component.
 */

import { defineMessages } from 'react-intl';
export const scope = 'containers.permissionScreen';
const messages = defineMessages({
  camera: {
    id: `${scope}.camera`
  },
  location: {
    id: `${scope}.location`
  },
  microphone: {
    id: `${scope}.microphone`
  },
  title: {
    id: `${scope}.title`
  },
  allow: {
    id: `${scope}.allow`
  },
  notYet: {
    id: `${scope}.notYet`
  },
  openPermissionsConfig: {
    id: `${scope}.openPermissionsConfig`
  }
});
export default messages;
//# sourceMappingURL=messages.js.map