/**
 * SignInCodeScreen messages
 *
 * This contains all the text for the SignInCodeScreen component.
 */

import { defineMessages } from 'react-intl';
export const scope = 'containers.promoCodeScreen';
const messages = defineMessages({
  title: {
    id: `${scope}.title`
  },
  resendCode: {
    id: `${scope}.resendCode`
  },
  codePlaceholder: {
    id: `${scope}.codePlaceholder`
  },
  invalidCode: {
    id: `${scope}.invalidCode`
  },
  codeResent: {
    id: `${scope}.codeResent`
  },
  next: {
    id: `${scope}.next`
  },
  promoCodeLength: {
    id: `${scope}.promoCodeLength`
  }
});
export default messages;
//# sourceMappingURL=messages.js.map