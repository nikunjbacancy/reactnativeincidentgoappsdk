/**
 * PermissionScreen Messages
 *
 * This contains all the text for the PermissionScreen component.
 */

import { defineMessages } from 'react-intl';
export const scope = 'containers.permissionLocationScreen';
const messages = defineMessages({
  title: {
    id: `${scope}.title`
  },
  location: {
    id: `${scope}.location`
  },
  settings: {
    id: `${scope}.settings`
  },
  enabled: {
    id: `${scope}.enabled`
  },
  allowAllTime: {
    id: `${scope}.allowAllTime`
  },
  allow: {
    id: `${scope}.allow`
  }
  //
});
export default messages;
//# sourceMappingURL=messages.js.map