/**
 * SignInScreen Messages
 *
 * This contains all the text for the SignInScreen component.
 */

import { defineMessages } from 'react-intl';
export const scope = 'containers.signInScreen';
const messages = defineMessages({
  agreeTos: {
    id: `${scope}.agreeTos`
  },
  phonePlaceholder: {
    id: `${scope}.phonePlaceholder`
  },
  sendCode: {
    id: `${scope}.sendCode`
  },
  title: {
    id: `${scope}.title`
  },
  countryCode: {
    id: `${scope}.countryCode`
  },
  tos: {
    id: `${scope}.tos`
  },
  privacyPolicy: {
    id: `${scope}.privacyPolicy`
  },
  privacy: {
    id: `${scope}.privacy`
  },
  phoneRequired: {
    id: `${scope}.phoneRequired`
  },
  phoneLength: {
    id: `${scope}.phoneLength`
  },
  agreeTosRequired: {
    id: `${scope}.agreeTosRequired`
  }
});
export default messages;
//# sourceMappingURL=messages.js.map