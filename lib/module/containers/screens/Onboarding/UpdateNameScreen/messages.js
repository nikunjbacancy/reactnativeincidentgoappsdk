/**
 * UpdateNameScreen messages
 *
 * This contains all the text for the UpdateNameScreen component.
 */

import { defineMessages } from 'react-intl';
export const scope = 'containers.updateNameScreen';
const messages = defineMessages({
  title: {
    id: `${scope}.title`
  },
  privacy: {
    id: `${scope}.privacy`
  },
  firstNamePlaceholder: {
    id: `${scope}.firstNamePlaceholder`
  },
  lastNamePlaceholder: {
    id: `${scope}.lastNamePlaceholder`
  },
  nameValidate: {
    id: `${scope}.nameValidate`
  },
  next: {
    id: `${scope}.next`
  }
});
export default messages;
//# sourceMappingURL=messages.js.map