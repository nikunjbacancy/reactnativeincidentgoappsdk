/**
 * AddCommentScreen Messages
 *
 * This contains all the text for the AddCommentScreen component.
 */

import { defineMessages } from 'react-intl';
export const scope = 'containers.addCommentScreen';
const messages = defineMessages({
  title: {
    id: `${scope}.title`
  },
  info: {
    id: `${scope}.info`
  },
  inputPlaceholder: {
    id: `${scope}.inputPlaceholder`
  },
  createTip: {
    id: `${scope}.createTip`
  },
  acknowledgment: {
    id: `${scope}.acknowledgment`
  }
});
export default messages;
//# sourceMappingURL=messages.js.map