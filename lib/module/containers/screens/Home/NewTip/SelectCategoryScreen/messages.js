/**
 * SelectCategoryScreen Messages
 *
 * This contains all the text for the SelectCategoryScreen component.
 */

import { defineMessages } from 'react-intl';
export const scope = 'containers.incidentCategoryScreen';
const messages = defineMessages({
  title: {
    id: `${scope}.title`
  },
  info: {
    id: `${scope}.info`
  },
  continue: {
    id: `${scope}.continue`
  }
});
export default messages;
//# sourceMappingURL=messages.js.map