/**
 * BottomMenu Messages
 *
 * This contains all the text for the BottomMenu component.
 */

import { defineMessages } from 'react-intl';
export const scope = 'bottomMenu';
const messages = defineMessages({
  myTips: {
    id: `${scope}.myTips`
  },
  contacts: {
    id: `${scope}.contacts`
  },
  beacons: {
    id: `${scope}.beacons`
  },
  myAccount: {
    id: `${scope}.myAccount`
  },
  notifications: {
    id: `${scope}.notifications`
  },
  organizations: {
    id: `${scope}.organizations`
  },
  feedbacks: {
    id: `${scope}.feedbacks`
  },
  mytips: {
    id: `${scope}.mytips`
  },
  panic: {
    id: `${scope}.panic`
  }
});
export default messages;
//# sourceMappingURL=messages.js.map