/**
 * SiteKey Messages
 *
 * This contains all the text for the VideoRecordScreen component.
 */

import { defineMessages } from 'react-intl';
export const scope = 'components.siteKeyModal';
const messages = defineMessages({
  siteKey: {
    id: `${scope}.siteKey`
  },
  status: {
    id: `${scope}.status`
  },
  onSite: {
    id: `${scope}.onSite`
  },
  checkInOnSite: {
    id: `${scope}.checkInOnSite`
  },
  checkOutOffSite: {
    id: `${scope}.checkOutOffSite`
  },
  description: {
    id: `${scope}.description`
  },
  noNetworkMessage: {
    id: `${scope}.noNetworkMessage`
  },
  tapToFetchLocation: {
    id: `${scope}.tapToFetchLocation`
  },
  selectLocation: {
    id: `${scope}.selectLocation`
  }
});
export default messages;
//# sourceMappingURL=messages.js.map