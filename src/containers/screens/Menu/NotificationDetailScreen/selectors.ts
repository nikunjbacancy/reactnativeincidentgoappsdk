// import { createSelector } from 'reselect';
// import { RootState } from 'store/types';

// import { initialState } from './reducer';

/**
 * Direct selector to the RecordScreen state domain
 */

// const deleteAccountStateDomain = (state: RootState) =>
//     state.deleteAccountState || initialState;

// /**
//  * Other specific selectors
//  */

// const makeSelectDeleteAccount = () =>
// createSelector(deleteAccountStateDomain, object => object);


// export { makeSelectDeleteAccount };
