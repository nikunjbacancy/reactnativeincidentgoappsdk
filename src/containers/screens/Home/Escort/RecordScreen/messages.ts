/**
 * SelectOrganizationScreen Messages
 *
 * This contains all the text for the SelectOrganizationScreen component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'containers.escort.recordScreen';

const messages = defineMessages({
  shareVideo: {
    id: `${scope}.shareVideo`,
  },
  audioOnly: {
    id: `${scope}.audioOnly`,
  },
  imSafe: {
    id: `${scope}.imSafe`,
  },
  panic: {
    id: `${scope}.panic`,
  },
  sharingAudio: {
    id: `${scope}.sharingAudio`,
  },
  escort: {
    id: `${scope}.escort`,
  },
  safeModalTitle: {
    id: `${scope}.safeModalTitle`,
  },
  safeModalInfo: {
    id: `${scope}.safeModalInfo`,
  },
  safeModalCommentPlaceholder: {
    id: `${scope}.safeModalCommentPlaceholder`,
  },
  safeModalCancel: {
    id: `${scope}.safeModalCancel`,
  },
  safeModalContinue: {
    id: `${scope}.safeModalContinue`,
  },
  endPanic: {
    id: `${scope}.endPanic`,
  },
  exitPanicModalTitle: {
    id: `${scope}.exitPanicModalTitle`,
  },
  exitPanicModalInfo: {
    id: `${scope}.exitPanicModalInfo`,
  },
  exitPanicModalPlaceholder: {
    id: `${scope}.exitPanicModalPlaceholder`,
  },
  exitPanicModalCancel: {
    id: `${scope}.exitPanicModalCancel`,
  },
  exitPanicModalConfirm: {
    id: `${scope}.exitPanicModalConfirm`,
  },
  exitPanicModalInputValidation: {
    id: `${scope}.exitPanicModalInputValidation`,
  },
  escortClosed: {
    id: `${scope}.escortClosed`,
  },
  escortClosedDetail: {
    id: `${scope}.escortClosedDetail`,
  },
  ok: {
    id: `${scope}.ok`,
  },
  timerExpired: {
    id: `${scope}.timerExpired`,
  },
});

export default messages;
