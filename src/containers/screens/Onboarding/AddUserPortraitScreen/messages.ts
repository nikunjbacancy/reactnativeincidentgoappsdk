/**
 * AddUserPortraitScreen messages
 *
 * This contains all the text for the AddUserPortraitScreen component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'containers.addUserPortraitScreen';

const messages = defineMessages({
  title: {
    id: `${scope}.title`,
  },
  info: {
    id: `${scope}.info`,
  },
  next: {
    id: `${scope}.next`,
  },
  optional: {
    id: `${scope}.optional`,
  },
});

export default messages;
